//Data Types
//1. String
let username="Zen";// ${username} this not allowed in "" or '' only in ``
console.log(username)

//backtriks(``)
let greeting=`hi ${username}`;
console.log(greeting)

//2.Number  it include both integer and decimal
let age = 20;
let price = 99.99;
 
//3. Boolean
let isOnline = false;
let isDarkMode = true;
// boolean have only 2 values true or false

//4. undefined
let city=
console.log(city)

//if you declare a variable but don't assign a value then it gives undefined

//5. Null 
// this null (empty) can be set when we want to add
let SelectedColour=null;

//6. object
// value of related collection
let person={
    name1:"Smith",
    age :25,
    isStudent :true
}